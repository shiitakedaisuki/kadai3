<!--post_bookにbook.newを入れること-->
  <!--バリデーションによるエラーメッセージを表示-->
  <% if post_book.errors.any? %>
      <%= post_book.errors.count %>errors prohibited this obj from being saved:
      <ul>
        <% post_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
        </ul>
  <% end %>
    <h2>New book</h2>
    <%= form_with model: post_book,url: books_path do |f| %>
      <h4>Title</h4>
      <%= f.text_field :title %>
      <h4>Opinion</h4>
      <%= f.text_area :body %>
      <div class="form-group"></div>
      <%= f.submit "Create Book",class:"btn btn-success" %>
    <% end %>